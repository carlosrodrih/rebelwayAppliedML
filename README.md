# Week 1 Assignment — Python Refresher

**Task:** Implement our own logic based on the class example, but adapted to a library system. Include testing using pytest.

---

## Setup

1. Install dependencies:

`pip install -r requirements.txt`

2. Run tests:

`pytest -v`
